# This is a TEMPLATE file.
# The application will replace the __PLACEHOLDER__ values on startup.

listener:
  host: __DB_HOST__
  port: __DB_PORT__
  max_connections: 100

remotesapi:
  port: 22222 # A different port for the remote API